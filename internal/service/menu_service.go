package service

/*func GetNotifications() (models.Notifications, error) {
	missedCalls := 0
	unreadMessages := 0

	missedCalls, err := database.GetMissedCalls()
	if err != nil {
		return models.Notifications{}, err
	}

	unreadMessages, err := database.GetUnreadMessages()
	if err != nil {
		return models.Notifications{}, err
	}

	return models.Notifications{
		MissedCalls:    missedCalls,
		UnreadMessages: unreadMessages,
	}, nil
}*/
